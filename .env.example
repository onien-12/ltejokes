REACT_APP_API_URL=http://localhost:3001
# Leave REACT_APP_VPN_API_URL unset. /api/vpn is same-origin in production (nginx)
# and in development (src/setupProxy.js), and the antifraud bundle only produces a
# payload same-origin. To point the dev proxy elsewhere, set VPN_BACKEND_URL.
